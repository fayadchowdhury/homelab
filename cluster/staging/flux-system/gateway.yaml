apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: staging-gateway
  namespace: flux-system
spec:
  interval: 5m
  path: ./cluster/staging/gateway
  prune: true
  wait: true
  timeout: 20m
  dependsOn:
    - name: staging-config
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  # healthChecks: # Health checks for components