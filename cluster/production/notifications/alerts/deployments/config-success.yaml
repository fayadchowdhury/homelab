apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: config-success
  namespace: flux-system
spec:
  eventMetadata:
    summary: "✅ Config Layer Deployment Successful"
    layer: "config"
  providerRef:
    name: slack-deployments
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: config
      namespace: flux-system
  inclusionList:
    - "configured$"
    - "created$"
    - "deleted$"
    - "Health check passed"