apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: core-success
  namespace: flux-system
spec:
  eventMetadata:
    summary: "✅ Core Layer Deployment Successful"
    layer: "core"
  providerRef:
    name: slack-deployments
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: core
      namespace: flux-system
  inclusionList:
    - "configured$"
    - "created$"
    - "deleted$"
    - "Health check passed"