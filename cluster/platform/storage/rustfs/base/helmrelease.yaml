apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rustfs
  namespace: rustfs
spec:
  interval: 30m
  timeout: 20m
  
  install:
    timeout: 60m
    remediation:
      retries: 3
  
  upgrade:
    crds: Skip
    remediation:
      retries: 3
  
  chart:
    spec:
      chart: rustfs
      version: '0.0.93'
      sourceRef:
        kind: HelmRepository
        name: rustfs
        namespace: rustfs
      interval: 12h
  
  values:
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
          - matchExpressions:
            - key: role/store
              operator: In
              values:
              - "true"

      podAntiAffinity:
        topologyKey: kubernetes.io/hostname
    
    ingress:
      enabled: false

    gatewayApi:
      enabled: true
      existingGateway:
        name: traefik-gateway
        namespace: traefik
      hostname: rustfs.home.com

    
    secret:
      existingSecret: rustfs-credentials

    storageclass:
      name: longhorn-slow-single-replica
      dataStorageSize: 256Mi
      logStorageSize: 128Mi
      secretName: internal-tls-cert