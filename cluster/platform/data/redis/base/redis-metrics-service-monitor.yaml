apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redis-metrics-service-monitor
  namespace: redis
spec:
  endpoints:
    - port: metrics
      interval: 30s
  selector:
    matchLabels:
      app: redis