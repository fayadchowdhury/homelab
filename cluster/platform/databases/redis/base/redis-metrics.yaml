apiVersion: v1
kind: Service
metadata:
  name: redis-metrics
  namespace: redis
  labels:
    app: redis
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9121
      targetPort: metrics
  selector:
    app: redis