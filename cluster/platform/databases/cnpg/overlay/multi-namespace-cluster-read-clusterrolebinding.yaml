apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cnpg-cluster-reader
subjects:
  - kind: ServiceAccount
    name: airflow-db-init
    namespace: airflow
roleRef:
  kind: ClusterRole
  name: cnpg-cluster-reader
  apiGroup: rbac.authorization.k8s.io